server:
  port: 8081

spring:
  application:
    name: student-service

  datasource:
    url: jdbc:postgresql://postgres_db:5432/studentdb
    username: postgres
    password: Hyder@014

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/
